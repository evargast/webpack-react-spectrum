import React from "react";
import { render, screen } from "test-utils";

import { {{name}} } from "./{{name}}";

// TODO: add proper tests for {{name}} component
test("{{name}} renders properly", () => {
render(
<{{name}} />);
expect(screen.getByText("{{name}}")).toBeInTheDocument();
});